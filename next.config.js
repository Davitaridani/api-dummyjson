/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['i.dummyjson.com', 'daisyui.com', 'cdn.dummyjson.com', 'pixahive.com', 'drive.google.com'],

  }
}

module.exports = nextConfig
